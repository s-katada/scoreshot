# nix / direnv
.direnv/
result
result-*

# macOS
.DS_Store

# node
node_modules/
dist/

# Tauri
# target と gen/schemas は src-tauri/.gitignore 側で無視される。
# gen/apple は Info.plist などの設定を含むためコミットする。

# pnpm check の中間生成物
.tmp-check/

# OMR のモデル (pnpm omr:models で取ってくる。100MB を超えるので git には入れない)
/public/models/*.onnx

# OMR の開発用スクリプトの中間生成物
.tmp-omr/
